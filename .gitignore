# Runtime data (SQLite DB, uploaded files, imports)
data/

# Go binaries built at repo root
/server
/migrate-rails

# Archives
*.zip
*.tar

# Local Rails migration test dumps (production data, never commit)
/tmp_rails_db/
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# macOS
.DS_Store

# Rails app leftovers in the shared workdir (main-branch runtime artifacts,
# only relevant while this branch shares a checkout with main)
/app/
/config/
/coverage/
/db/
/dev-log/
/docs/
/export/
/log/
/storage/
/test/
/tmp/
/.codex/
/.kamal/
/.wrangler/
